import { DifficultyDirective } from './difficulty.directive';

describe('DifficultyDirective', () => {
  it('should create an instance', () => {
    const directive = new DifficultyDirective();
    expect(directive).toBeTruthy();
  });
});
